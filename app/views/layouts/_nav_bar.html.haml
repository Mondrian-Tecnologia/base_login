%nav.navbar.navbar-expand.navbar-light.bg-white.topbar.mb-4.static-top.shadow
  / Sidebar Toggle (Topbar)
  -#%button#sidebarToggleTop.btn.btn-link.d-md-none.rounded-circle.mr-3
  -#  %i.fa.fa-bars
  -#/ Topbar Search
  -#= form_with model: nil, url: polymorphic_url([current_usuario.entidade_type.downcase, :dutes]),
  -#            html: { class: 'd-none d-sm-inline-block form-inline mr-auto ml-md-3 my-2 my-md-0 mw-100 navbar-search', method: :get } do
  -#  .input-group
  -#    = text_field_tag :placa, params[:placa] || @dute&.placa, class: 'form-control bg-light border-0 small placa', placeholder: 'Buscar DUTe por Placa...',
  -#                                           pattern: pattern(:placa)
  -#    .input-group-append
  -#      %button.btn.btn-primary{:type => "submit"}
  -#        %i.fas.fa-search.fa-sm

  / Topbar Navbar
  %ul.navbar-nav.ml-auto
    / Nav Item - User Information
    %li.nav-item.dropdown.no-arrow
      = icon_conexao_detran
    %li.nav-item.dropdown.no-arrow
      = link_to '#', id: 'userDropdown', class: 'nav-link dropdown-toggle', role: 'button',
                     aria: { expanded: false, haspopup: true },
                     data: { toggle: 'dropdown' } do
        %span.d-none.d-lg-inline
          = truncate(current_usuario.username, length: 20, escape: false)
        .arrow-down
        = image_tag usuario_avatar(current_usuario, 100), class: "img-profile rounded-circle"

      / Dropdown - User Information
      .dropdown-menu.dropdown-menu-right.shadow.animated--grow-in{"aria-labelledby" => "userDropdown"}
        = link_to edit_usuario_registration_path, class: 'dropdown-item' do
          = svg_icon :key, class: 'dropdown icon'
          Alterar Senha
        = link_to destroy_usuario_session_path, method: :delete, class: 'dropdown-item' do
          = svg_icon :logout, class: 'dropdown icon'
          Desconectar
